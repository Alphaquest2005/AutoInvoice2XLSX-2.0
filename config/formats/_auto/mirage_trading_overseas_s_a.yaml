# Auto-generated format spec for mirage_trading_overseas_s_a
# Generated on 2026-04-06T13:24:22.789138
# Will be promoted to formats/ on successful processing

name: mirage_trading_overseas_s_a
version: "1.0"
description: "Invoice format for Mirage Trading Overseas S.A. with itemized lines including quantity, description, cartons, weight, model, price, and total."

detect:
  all_of:
    - "MIRAGE TRADING OVERSEAS S.A."
    - "ZONA LIBRE DE COLON"
  any_of:
    - "INVOICE No."
    - "TOTAL USD"

metadata:
  invoice_number:
    patterns:
      - 'INVOICE No\.(\d+)'
    required: true
  date:
    patterns:
      - 'DATE:\s*(\d{2}/\d{2}/\d{4})'
  supplier_name:
    value: "MIRAGE TRADING OVERSEAS S.A."
  country_code:
    value: "PA"
  total:
    patterns:
      - 'TOTAL USD\s+([\d,]+\.\d{2})'
    type: currency

items:
  strategy: line
  line:
    pattern: '^(\d+)\s+(.+?)\s+(\d+)\s+[\d,]+\.\d{2}\s+[\w-]+\s+([\d,]+\.\d{2})\s+([\d,]+\.\d{2})\s*$'
    field_map:
      quantity: 1
      description: 2
      unit_price: 4
      line_total: 5
    types:
      quantity: integer
      unit_price: currency
      line_total: currency
    skip_patterns:
      - '^QTY\s+DESCRIPTION'
      - '^\d+\s+CTNS$'
      - '^[\d,]+\.\d{2}\s+WEIGHT$'
      - '^[\d,]+\.\d{2}\s+CUB\.$'
      - '^SUB-TOTAL'
      - '^DOCUMENTS'
      - '^TOTAL USD'

sections:
  items_start:
    - "QTY DESCRIPTION"
  items_end:
    - "SUB-TOTAL FOB"

validation:
  min_items: 1
  max_item_price: 10000