# Auto-generated format spec for aurola_store
# Generated by LLM on 2026-02-12T23:14:25.141047

name: aurola_store
version: "1.0"
description: "Aurola Store Purchase Confirmation Invoice"

detect:
  all_of:
    - "AUROLA"
    - "Purchase confirmation"
  any_of:
    - "Order Code"
    - "Get special goods and gifts"

metadata:
  invoice_number:
    patterns:
      - 'Order Code ([A-Z0-9]+)'
      - 'Order from AUROLA ([A-Z0-9]+)'
    required: true
  date:
    patterns:
      - 'Date: ([0-9]{1,2} [A-Za-z]+ [0-9]{4})'
  supplier_name:
    value: "AUROLA"
  country_code:
    value: "US"
  total:
    patterns:
      - 'Total Due \$([\d,]+\.\d{2})'
    type: currency
  discount:
    patterns:
      - 'Discount\s+\S+\s+-?\$?([\d,]+\.\d{2})'
      - 'Discount\s*:?\s*-?\$?([\d,]+\.\d{2})'
    type: currency
  shipping:
    patterns:
      - 'Shipping\s+\$([\d,]+\.\d{2})'
      - 'Shipping\s+(FREE)'
    type: currency
    default: 0
  tax:
    patterns:
      - 'Tax\s+\$([\d,]+\.\d{2})'
    type: currency
    default: 0

items:
  strategy: line
  line:
    pattern: '([A-Za-z][\w\s\/]+) ([A-Z][a-z]+ \/ [A-Z]+) \$([\d,]+\.\d{2})'
    field_map:
      description: 1
      details: 2
      unit_price: 3
    types:
      unit_price: currency
    skip_patterns:
      - '^Subtotal'
      - '^Shipping'
      - '^Tax'
      - '^Discount'
      - '^Total Due'
      - '^you saved'
      - '^Shipping Address'
      - '^Billing Address'
      - '^Shipping Method'
      - '^Payment Method'
      - '^Get in Touch'
      - '^Order Code'
      - '^Items Price'
      - '^Quantity'
      - '^\s*€'

sections:
  items_start:
    - "Order Code"
    - "Items Price"
  items_end:
    - "Shipping Address"
    - "Total Due"

validation:
  min_items: 1
  max_item_price: 10000