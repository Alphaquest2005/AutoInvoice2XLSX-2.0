# Auto-generated format spec for unknown_
# Generated on 2026-04-06T10:28:52.531724
# Will be promoted to formats/ on successful processing

name: azari_factory
version: "1.0"
description: "Invoice format for AZARI FACTORY, S.A. featuring itemized lists with SKU, description, quantity, unit price, and totals."

detect:
  all_of:
    - "AZARI FACTORY, S.A."
    - "ZONA LIBRE DE COLON"
  any_of:
    - "INVOICE"
    - "FACTURA"

metadata:
  invoice_number:
    patterns:
      - 'INVOICE\s+([\d-]+)'
    required: true
  date:
    patterns:
      - 'DATE\s+(\d{2}/\d{2}/\d{4})'
  supplier_name:
    value: "AZARI FACTORY, S.A."
  country_code:
    value: "PA"
  total:
    patterns:
      - 'TOTAL\s+([\d,]+\.\d{2})'
    type: currency

items:
  strategy: line
  line:
    pattern: '^([A-Z0-9\-]+)\s+(.+?)\s+(\d+)\s+([\d,]+\.\d{2})\s+([\d,]+\.\d{2})$'
    field_map:
      sku: 1
      description: 2
      quantity: 3
      unit_price: 4
      line_total: 5
    types:
      quantity: integer
      unit_price: currency
      line_total: currency
    skip_patterns:
      - '^SUB TOTAL'
      - '^DOCUMENT'
      - '^TOTAL'
      - '^Box'
      - '^\d+\s+Box'

sections:
  items_start:
    - "Dispatched"
  items_end:
    - "SUB TOTAL"

validation:
  min_items: 1