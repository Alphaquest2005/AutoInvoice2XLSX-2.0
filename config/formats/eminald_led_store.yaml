# Auto-generated format spec for unknown_eminald
# Generated by LLM on 2026-02-13T05:08:39.809406

name: eminald_led_store
version: "1.0"
description: "Invoice format for Emerald/LED Store orders from Alibaba marketplace"

detect:
  all_of:
    - "Marketplace Facilitator Alibaba.com"
    - "Total inclusive of Sales Tax"

metadata:
  invoice_number:
    patterns:
      - 'Invoice No\.\s+([A-Z0-9]+)'
    required: true
  date:
    patterns:
      - 'Invoice Date\s+(\d{4}-\d{2}-\d{2})'
  supplier_name:
    value: "Emerald LED Store"
  country_code:
    value: "US"
  total:
    patterns:
      - 'Total inclusive of\s+Sales Tax\).*?\((USD)\)'
    type: currency

items:
  strategy: line
  line:
    pattern: '^([A-Z][A-Z0-9][\w\s\-\.\/]+)\s+([A-Z0-9\-\s]+)$'
    field_map:
      description: 1
      sku: 2
    types:
      sku: string
    skip_patterns:
      - '^Invoice '
      - 'Supplier name'
      - 'Customer Name'
      - 'Marketplace'
      - 'Delivery Address'
      - 'Invoice Date'
      - 'Transaction'
      - '^\('
      - '^As required'
      - '^Total'

sections:
  items_start:
    - "Delivery Address"
  items_end:
    - "Total inclusive"

validation:
  min_items: 1