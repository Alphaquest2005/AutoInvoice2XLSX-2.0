# FashionNova invoice format v11 (FIXED)
# Fixed: proper look_ahead structure and escaped backslash

name: fashionnova_ecommerce
version: "11.0"
description: "FashionNova invoice - simple line-based extraction"

detect:
  all_of:
    - "Order #"
    - "Date Placed:"
    - "Total"
    - "Size:"
    - "Qty"
  any_of:
    - "NEED HELP?"
    - "Payment method:"

metadata:
  invoice_number:
    patterns:
      - 'Order #(\d+)'
    required: true
  date:
    patterns:
      - 'Date Placed:\s*([A-Za-z]+ \d{1,2}, \d{4})'
  supplier_name:
    value: "FashionNova"
  country_code:
    value: "US"
  total:
    patterns:
      - 'Total\s+\$?([\d,]+\.\d{2})'
    type: currency

items:
  strategy: simple
  simple:
    # Match product name lines
    pattern: '^(?P<description>[A-Z][A-Za-z0-9\s\-]+)$'
    min_length: 10
    field_map:
      description: 1
    skip_patterns:
      - '^Order #'
      - '^Date Placed:'
      - '^Size:'
      - '^Qty:'
      - '^Price:'
      - '^Subtotal'
      - '^Total\s+\$?'
      - '^Shipping'
      - '^Discount'
      - '^Payment method'
      - '^Delivery Address'
      - '^Billing Address'
      - '^FASHION'
      - '^Visit'
      - '^Shop'
      - '^Going Viral'
      - '^\s*$'
      - '^\$[\d,]+\.\d{2}$'
    # Extract price/qty from following lines
    price_pattern: '^Price:\s*\$(?P<unit_price>[\d,]+\.\d{2})$'
    quantity_pattern: '^Qty:\s*(?P<quantity>\d+)$'
    types:
      unit_price: currency
      quantity: integer
    generated_fields:
      sku: "FN-{index}"

sections:
  items_start:
    - "Going Viral"
  items_end:
    - "Subtotal"
    - "Delivery Address"

validation:
  min_items: 1
  max_item_price: 500
