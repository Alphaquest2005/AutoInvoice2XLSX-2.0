# Auto-generated format spec for unknown_invoice
# Generated by LLM on 2026-02-13T05:01:54.709547

name: ganesha_enterprises_llc
version: "1.0"
description: "Invoice format for Ganesha Enterprises LLC featuring freight, line items with case/inner details, and a specific footer."

detect:
  all_of:
    - "GANESHA ENTERPRISES LLC"
    - "5200 SIGSTROM DRIVE"
  any_of:
    - "INVOICE COPY"

metadata:
  invoice_number:
    patterns:
      - 'Invoice no[^|]+\|\s*([A-Z0-9/]+)'
      - '(G[/\s]*\d{4,})'
      - 'Sales Order #\s*\n?\s*G?\s*(\d{5,})'
    required: false
  date:
    patterns:
      - '(\d{1,2}\s*[/(\s]\s*\d{1,2}\s*[/)\s]\s*\d{4})'
    required: false
  supplier_name:
    value: "GANESHA ENTERPRISES LLC"
  country_code:
    value: "US"
  total:
    patterns:
      - 'TOTAL\s+([\d,]+\.\d{2})\$'
      - '([\d,]+\.\d{2})\$\s*\|?\s*$'
    type: currency

items:
  strategy: line
  line:
    # Ganesha item lines: DESCRIPTION QTY UNIT_CODE UNIT_TYPE PRICE$
    # e.g. "OTP.RMDGUTKHA,JUNE 50 NV cs 80.00$ |"
    pattern: '^(.+?)\s+(\d+)\s+\w+\s+\w+\s+([\d,.]+)\$'
    field_map:
      description: 1
      quantity: 2
      total_cost: 3
    types:
      quantity: integer
      total_cost: currency
    generated_fields:
      unit_cost: "total_cost / quantity"
    skip_patterns:
      - '^Item Description'
      - '^Freight$'
      - '^Site Qty'
      - 'TOTAL'

sections:
  items_start:
    - "Ship To"
    - "Name/Address"
  items_end:
    - "TOTAL"
    - "IF YO"
    - "CHECK IS"

validation:
  min_items: 1