# Auto-generated format spec for unknown_
# Generated by LLM on 2026-02-13T04:09:14.093632

name: "Generic Web Order (Unknown)"
version: "1.0"
description: "Generic web order invoice format with item details, quantities, and pricing."

detect:
  all_of:
    - "Delivery instructions"
    - "Review item"
  any_of:
    - "Return eligible until"
    - "Start a return"

metadata:
  invoice_number:
    patterns:
      - 'Order#[\s]+(\d{6}-\d{8})'
    required: true
  date:
    patterns:
      - '(\w{3}\s\d{1,2},\s\d{4})\s+order'
  supplier_name:
    value: "Unknown Supplier"
  country_code:
    value: "US"
  total:
    patterns:
      - 'Total\s+\$?([\d,]+\.\d{2})'
    type: currency

items:
  strategy: line
  line:
    pattern: '^\s*(.+?)\s+\$([\d,]+\.\d{2})(?:/ea)?\s*'
    field_map:
      description: 1
      unit_price: 2
    types:
      unit_price: currency
    skip_patterns:
      - '^Subtotal'
      - '^Tax'
      - '^Total'
      - '^Payment method'
      - '^\s+\d+\s+items?\s*$'
      - 'Delivery instructions'
      - '^Ending in'

sections:
  items_start:
    - "Review item"
    - "Delivery Address"
  items_end:
    - "Payment method"
    - "Subtotal"
    - "Total"

validation:
  min_items: 1