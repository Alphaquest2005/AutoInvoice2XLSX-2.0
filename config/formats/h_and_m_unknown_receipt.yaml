# Auto-generated format spec for unknown_receipt
# Generated by LLM on 2026-02-13T04:10:44.010986

name: h_and_m_unknown_receipt
version: "1.0"
description: "H&M style receipt with columns for Art.No, Desc, Size, Colour, Qty, Unit Price, Discount, and Total Price"

detect:
  all_of:
    - "RECEIPT DATE"
    - "ORDER SUMMARY"
    - "ART.NO"
    - "UNIT PRICE"
  any_of:
    - "MERCHANDISE TOTAL:"
    - "DISCOUNT:"

metadata:
  invoice_number:
    patterns:
      - 'RECEIPT NUMBER\s+(\d+)'
    required: true
  date:
    patterns:
      - 'RECEIPT DATE\s+(\d{1,2}\s+[A-Za-z]+\s+\d{4})'
      - 'ORDER DATE\s+(\d{1,2}\s+[A-Za-z]+\s+\d{4})'
  total:
    patterns:
      - 'TOTAL:\s+\$?([\d,]+\.\d{2})'
    type: currency

items:
  strategy: line
  line:
    pattern: '^([\d\w]+)\s+(.+?)\s+([\w\/]+)\s+([\w\s\-\)\(]+)\s+(\d+)\s+\$?([\d,]+\.\d{2})\s+[\+\-]?\$?[\d,]+\.\d{2}\s+\$?([\d,]+\.\d{2})'
    field_map:
      sku: 1
      description: 2
      size: 3
      color: 4
      quantity: 5
      unit_price: 6
      total_price: 7
    types:
      quantity: integer
      unit_price: currency
      total_price: currency
    skip_patterns:
      - '^ART\.NO'
      - '^(MERCHANDISE|SUBTOTAL|SHIPPING|SALES TAX|TOTAL|DISCOUNT):?'
      - '^QA-code'
      - '^Note, you Cannot reply'
      - '^For more information'
      - '^You always Have'
      - '^\s*$'

sections:
  items_start:
    - "ART.NO"
    - "ORDER SUMMARY"
  items_end:
    - "MERCHANDISE TOTAL"

validation:
  min_items: 1