# Auto-generated format spec for la_parts_order_number
# Generated on 2026-04-18T00:32:59.753677
# Will be promoted to formats/ on successful processing

name: la_parts_order_number
version: "1.0"
description: "Multi-vendor auto parts order invoice with individual order numbers per item"

detect:
  all_of:
    - "Order number"
    - "Items bought from"
  any_of:
    - "Shipping service"
    - "Item price"

metadata:
  invoice_number:
    patterns:
      - 'Order\s*number\s*:\s*(\d+\s*-\s*\d+\s*-\s*\d+)'
    required: true
  date:
    patterns:
      - 'Paid\s*on\s*([A-Z][a-z]+\s+\d{1,2}\s*,\s*\d{4})'
  supplier_name:
    value: "la_parts_order_number"
  country_code:
    value: "US"
  total:
    patterns:
      - 'Order\s*total\s*\$?([\d,]+\.\d{2})'
    type: currency

items:
  strategy: line
  line:
    pattern: '^(\d+)\s+(.+?)\s+\(\s*(\d+)\s*\)\s+[\w\s]+\s+\$([\d,]+\.\d{2})\s*$'
    field_map:
      quantity: 1
      description: 2
      sku: 3
      unit_price: 4
    types:
      quantity: integer
      unit_price: currency
    skip_patterns:
      - '^Items bought from'
      - '^Order number'
      - '^Quantity\s+Item'
      - '^Paid on'
      - '^Payment method'
      - '^Order total'
      - '^Learn more'
      - '^\d{5}\s*,\s*'
      - '^$'

sections:
  items_start:
    - "Order number"
  items_end:
    - "Order total"

validation:
  min_items: 1
  max_item_price: 10000