# Auto-generated format spec for shein
# Generated by LLM on 2026-02-13T04:01:00.831304

name: shein
version: "1.0"
description: "SHEIN US Services, LLC. Invoice Format"
detect:
  all_of:
    - "SHEIN US Services, LLC"
    - "Invoice Detail"
  any_of:
    - "Sales Invoice"
    - "Grand Total:"
    - "Item(s) Subtotal:"

metadata:
  invoice_number:
    patterns:
      - 'Invoice No\:\:\s*(?:INVUS\d+\s+)?(\d+)'
    required: true
  date:
    patterns:
      - 'Invoice Date\:\:\s*(\d{4}-\d{2}-\d{2})'
  supplier_name:
    value: "SHEIN"
  country_code:
    value: "US"
  total:
    patterns:
      - 'Grand Total\:\:\s*([\d,]+\.\d{2})'
    type: currency

items:
  strategy: line
  line:
    pattern: '^(.+)[:\s]+(\d+)\s+([\d,]+\.\d{2})$'
    field_map:
      description: 1
      quantity: 2
      unit_price: 3
    types:
      quantity: integer
      unit_price: currency
    skip_patterns:
      - '^Item\(s\) Subtotal\:'
      - '^Shipping/Handling\:'
      - '^Sales Tax\:'
      - '^Grand Total\:'
      - '^Order Information'
      - '^Sold By\:'
      - '^Address\:'
      - '^\s*\|'
      - '^\s*$'
    generated_fields:
      sku: "SHEIN-{index}"

sections:
  items_start:
    - "Invoice Detail"
  items_end:
    - "Item(s) Subtotal"

validation:
  min_items: 1
  max_item_price: 10000