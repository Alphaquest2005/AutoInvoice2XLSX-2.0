# Auto-generated format spec for shein
# Generated by LLM on 2026-02-13T04:48:12.703622

name: shein_invoice
version: "1.0"
description: "Invoice format for SHEIN US Services, LLC featuring order info, seller details, and itemized lists."

detect:
  all_of:
    - "SHEIN US Services, LLC"
    - "Sales Invoice"
    - "777 S Alameda St"
  any_of:
    - "Invoice Detail"
    - "Order Information"

metadata:
  invoice_number:
    patterns:
      - 'Invoice No\.\:?\s*(INVUS[\d\s]+)'
    required: true
  date:
    patterns:
      - 'Invoice Date\:\s*(\d{4}-\d{2}-\d{2})'
  supplier_name:
    value: "SHEIN"
  country_code:
    value: "US"
  total:
    patterns:
      - 'Grand Total\:\s*[\$¥]?[\s]?([\d,]+\.?\d{0,2})'
    type: currency

items:
  strategy: line
  line:
    pattern: '^(.+?)(?:\s{2,}|\\t)(\d+)\s+([\d,]+\.?\d{2})$'
    field_map:
      description: 1
      quantity: 2
      unit_price: 3
    types:
      quantity: integer
      unit_price: currency
    skip_patterns:
      - '^Item\(s\) Subtotal'
      - '^Shipping/Handling'
      - '^Sales Tax'
      - '^Grand Total'
      - '^Order Number'
      - '^Sold By'
      - '^Address'
      - '^Invoice No'
      - '^Bill To'
      - '^Delivery Address'
      - '^\d{4}-\d{2}-\d{2}'

sections:
  items_start:
    - "Invoice Detail"
  items_end:
    - "Grand Total"

validation:
  min_items: 1