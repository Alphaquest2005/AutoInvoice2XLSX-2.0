# Auto-generated format spec for shein
# Generated by LLM on 2026-02-13T05:11:39.858460

name: shein_invoice
version: "1.0"
description: "Invoice format for SHEIN US orders"

detect:
  all_of:
    - "SHEIN US Services, LLC"
    - "Sales Invoice"
  any_of:
    - "Order Number"
    - "Sold By"

metadata:
  invoice_number:
    patterns:
      - 'Invoice No\.\:\s*([A-Z0-9]+)'
    required: true
  date:
    patterns:
      - 'Invoice Date\:\s*(\d{4}-\d{2}-\d{2})'
      - 'Order Date\:\s*(\d{4}-\d{2}-\d{2})'
  supplier_name:
    value: "SHEIN"
  country_code:
    value: "US"
  total:
    patterns:
      - 'Grand Total\:\s*([\d,]+\.\d{2})'
    type: currency

items:
  strategy: line
  line:
    pattern: '^([^\n]+)'
    field_map:
      description: 1
    types:
      description: string
    skip_patterns:
      - '^Bill To'
      - '^Billing Address'
      - '^Delivery Address'
      - '^Order Information'
      - '^Order Number'
      - '^Seller Information'
      - '^Sold By'
      - '^Invoice Detail'
      - '^SHEIN US Services'
      - '^Invoice No'
      - '^Invoice Date'
      - '^Order Date'
      - '^Address'
      - '^China'
      - '^Item\(s\) Subtotal'
      - '^Shipping'
      - '^Sales Tax'
      - '^Grand Total'
      - '^Sales Invoice'
      - '^\s*\d+\.\d{2}\s*$' # Skip lines that are just prices
      - '^\d+$' # Skip lines that are just quantity numbers
    generated_fields:
      sku: "SHEIN-{index}"

sections:
  items_start:
    - "Order Number"
    - "Sold By"
  items_end:
    - "Item(s) Subtotal"

validation:
  min_items: 1