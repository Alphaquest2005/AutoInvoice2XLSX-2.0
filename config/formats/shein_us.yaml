# Auto-generated format spec for shein
# Generated by LLM on 2026-02-13T03:58:52.369308

name: shein_us
version: "1.0"
description: "SHEIN US Services Invoice Format"

detect:
  all_of:
    - "SHEIN US Services, LLC."
    - "Invoice No.:"
  any_of:
    - "Sales Invoice"
    - "Invoice Detail"

metadata:
  invoice_number:
    patterns:
      - 'INVUS\s*(\d+)'
    required: true
  date:
    patterns:
      - 'Invoice Date:\s*(\d{4}-\d{2}-\d{2})'
      - '(\d{4}-\d{2}-\d{2})'
  supplier_name:
    value: "SHEIN"
  country_code:
    value: "US"
  total:
    patterns:
      - 'Grand Total:\s*([\d,]+\.\d{2})'
      - 'Grand Total:\s*\$?\s*([\d,]+\.\d{2})'
    type: currency

items:
  strategy: line
  line:
    pattern: '^(.+)\s+(\d+)\s+([\d,]+\.\d{2})\s*$'
    field_map:
      description: 1
      quantity: 2
      unit_price: 3
    types:
      quantity: integer
      unit_price: currency
    skip_patterns:
      - '^(Item\(s\) Subtotal|Shipping/Handling|Sales Tax|Grand Total|Order Information|Seller Information|Description Quantity)'
      - '^(Bill To:|Invoice No.:|Billing Address:|Invoice Date:|Delivery Address:)'
      - '^(Order Number:|Order Date:|Sold By:|Address:|httne:)'
      - '^(SHEIN US Services)'
      - '^\s*$'
      - 'roundcube'
      - '777 S Alameda'
      - 'Los Angeles, CA'

sections:
  items_start:
    - "Invoice Detail"
    - "Description"
  items_end:
    - "Item(s) Subtotal"
    - "Grand Total"

validation:
  min_items: 1
  max_item_price: 10000