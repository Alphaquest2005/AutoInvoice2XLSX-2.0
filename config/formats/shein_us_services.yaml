# Auto-generated format spec for shein
# Generated by LLM on 2026-02-13T04:16:27.719446

name: shein_us_services
version: "1.0"
description: "Invoice format for SHEIN US Services, LLC featuring roundcube email headers and specific item alignment."

detect:
  all_of:
    - "SHEIN US Services, LLC"
    - "Sales Invoice"
  any_of:
    - "roundcube"
    - "Invoice Detail"

metadata:
  invoice_number:
    patterns:
      - 'INVUS\s*(\d{20})'
    required: true
  date:
    patterns:
      - 'Invoice Date:\s*(\d{4}-\d{2}-\d{2})'
  supplier_name:
    value: "SHEIN US Services, LLC"
  country_code:
    value: "US"
  total:
    patterns:
      - 'Grand Total:\s*([\d,]+\.\d{2})'
    type: currency

items:
  strategy: line
  line:
    pattern: '^(?P<description>[A-Z\d].*?)\s+(?P<quantity>\d+)\s+(?P<unit_price>[\d,]+\.\d{2})\s*$'
    field_map:
      description: description
      quantity: quantity
      unit_price: unit_price
    types:
      quantity: integer
      unit_price: currency
    skip_patterns:
      - '^Item\(s\) Subtotal:'
      - '^Shipping/'
      - '^Sales Tax:'
      - '^Grand Total:'
      - '^Description'
      - '^httne'
      - '^414$'

sections:
  items_start:
    - "Invoice Detail"
  items_end:
    - "Grand Total"

validation:
  min_items: 1
  max_item_price: 10000