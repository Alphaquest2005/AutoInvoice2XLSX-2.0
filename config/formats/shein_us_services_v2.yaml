# Auto-generated format spec for shein
# Generated by LLM on 2026-02-13T04:49:55.154028

name: shein_us_services
version: "1.0"
description: "Invoice format for SHEIN US Services, LLC featuring Invoice Detail sections and specific footer/contact info."

detect:
  all_of:
    - "SHEIN US Services, LLC"
    - "Invoice Detail"
    - "777 S Alameda St"
  any_of:
    - "Grand Total:"
    - "Invoice No.:"

metadata:
  invoice_number:
    patterns:
      - 'INV[A-Z0-9]{21,24}'
    required: true
  date:
    patterns:
      - '\d{4}-\d{2}-\d{2}'
    required: true
  supplier_name:
    value: "SHEIN"
  country_code:
    value: "US"
  total:
    patterns:
      - 'Grand Total:\s*([\d,]+\.\d{2})'
    type: currency

items:
  strategy: line
  line:
    pattern: '^([\d\w]+(?:[,\s][\d\w]+)*.*?)\s+(\d+)\s+([\d,]+\.\d{2})\s*$'
    field_map:
      description: 1
      quantity: 2
      unit_price: 3
    types:
      quantity: integer
      unit_price: currency
    skip_patterns:
      - '^Item\(s\) Subtotal:'
      - '^Shipping/Handling:'
      - '^Sales Tax:'
      - '^Grand Total:'
      - '^Order Information'
      - '^Seller Information'
      - '^Invoice Detail'

sections:
  items_start:
    - "Invoice Detail"
  items_end:
    - "Item(s) Subtotal:"

validation:
  min_items: 1
  max_item_price: 10000