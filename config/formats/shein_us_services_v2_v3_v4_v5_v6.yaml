# Auto-generated format spec for shein
# Generated by LLM on 2026-02-13T04:58:10.186761

name: shein_us_services
version: "1.0"
description: "Invoice format for SHEIN US Services, LLC featuring itemized lists with 'Sales Invoice' headers or inline table data."

detect:
  all_of:
    - "SHEIN US Services, LLC."
    - "Bill To:"
    - "Invoice Detail"
  any_of:
    - "Sales Invoice"
    - "Grand Total:"

metadata:
  invoice_number:
    patterns:
      - 'Invoice No\.:\s*(INVUS[A-Z0-9]+)'
      - 'Invoice No\.:\s*([A-Z0-9]+)'
    required: true
  date:
    patterns:
      - 'Invoice Date:\s*(\d{4}-\d{2}-\d{2})'
      - 'Order Date:\s*(\d{4}-\d{2}-\d{2})'
    required: true
  supplier_name:
    value: "SHEIN US Services, LLC."
  country_code:
    value: "US"
  total:
    patterns:
      - 'Grand Total:\s*([\d,]+\.\d{2})'
    type: currency

items:
  strategy: line
  line:
    pattern: '^(.+?)(?:\s+(\d+)\s+)?([\d,]+\.\d{2})\s*$'
    field_map:
      description: 1
      quantity: 2
      unit_price: 3
    types:
      quantity: integer
      unit_price: currency
    skip_patterns:
      - '^Item\(s\) Subtotal:'
      - '^Shipping/Handling:'
      - '^Sales Tax:'
      - '^Grand Total:'
      - '^Amount\(USD\)'
      - '^Order Number:'
      - '^Order Date:'
      - '^Sold By:'
      - '^Address:'
      - '^$'
      - '\s{5,}'  # Skip lines with only whitespace
    generated_fields:
      sku: "SHEIN-{invoice_number}-{index}"

sections:
  items_start:
    - "Invoice Detail"
    - "Sales Invoice"
  items_end:
    - "Grand Total"

validation:
  min_items: 1