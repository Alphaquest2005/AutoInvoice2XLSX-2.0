# Auto-generated format spec for shein
# Generated by LLM on 2026-02-13T05:00:45.799563

name: shein_us_services
version: "1.0"
description: "Invoice format for SHEIN US Services, LLC."

detect:
  all_of:
    - "SHEIN US Services, LLC."
    - "Sales Invoice"
    - "Invoice No.:"
    - "Grand Total:"

metadata:
  invoice_number:
    patterns:
      - 'Invoice No\.:\s*INVUS\s*([0-9]+)'
      - 'INVUS\s*([0-9]{14,})'
    required: true
  date:
    patterns:
      - 'Invoice Date:\s*([0-9]{4}-[0-9]{2}-[0-9]{2})'
  supplier_name:
    value: "SHEIN US Services, LLC."
  country_code:
    value: "US"
  total:
    patterns:
      - 'Grand Total:\s*([\d,]+\.\d{2})'
    type: currency

items:
  strategy: line
  line:
    pattern: '^(?P<description>.+?)\s+(?P<quantity>\d+)\s+(?P<unit_price>[\d,]+\.\d{2})\s*$'
    field_map:
      description: description
      unit_price: unit_price
      quantity: quantity
    types:
      quantity: integer
      unit_price: currency
    skip_patterns:
      - '^Item\(s\) Subtotal'
      - '^Shipping/Handling'
      - '^Sales Tax'
      - '^Grand Total'
      - '^Total'
      - '^Price'
      - '^\s*$'

sections:
  items_start:
    - "Invoice Detail"
  items_end:
    - "Grand Total:"
    - "Item(s) Subtotal:"

validation:
  min_items: 1
  max_item_price: 10000