# Auto-generated format spec for shein
# Generated by LLM on 2026-02-13T05:03:28.269096

name: shein_us_services
version: "1.0"
description: "Invoice format for SHEIN US Services, LLC with standard header, billing details, and variable item layout."

detect:
  all_of:
    - "SHEIN US Services, LLC."
    - "Sales Invoice"
    - "Invoice No.:"

metadata:
  invoice_number:
    patterns:
      - 'Invoice No\.\s*:\s*(INVUS\d+\s+\d+)'
      - 'INVUS(\d{8}\s\d{7})'
    required: true
  date:
    patterns:
      - 'Invoice Date:\s*(\d{4}-\d{2}-\d{2})'
  supplier_name:
    value: "SHEIN US Services, LLC."
  country_code:
    value: "US"
  total:
    patterns:
      - 'Grand Total:\s*([\d,]+\.\d{2})'
    type: currency

items:
  strategy: line
  line:
    pattern: '(.*?)(?:\s+(\d+))?\s+([\d,]+\.\d{2})\s*$'
    field_map:
      description: 1
      quantity: 2
      unit_price: 3
    types:
      quantity: integer
      unit_price: currency
    skip_patterns:
      - '^Item\(s\) Subtotal:'
      - '^Shipping/Handling:'
      - '^Sales Tax:'
      - '^Grand Total:'
      - '^Total:'
      - '^\d+\.\d+\s+\d+\.\d+' # Matches lines with two numbers that look like price repeats, not items

sections:
  items_start:
    - "Invoice Detail"
  items_end:
    - "Item(s) Subtotal"
    - "Grand Total"

validation:
  min_items: 1