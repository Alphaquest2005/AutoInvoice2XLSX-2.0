# Auto-generated format spec for shein
# Generated by LLM on 2026-02-13T08:50:26.818786

name: shein_us_services
version: "1.0"
description: "Invoice format for SHEIN US Services, LLC featuring invoice details, customer info, and line items."

detect:
  all_of:
    - "SHEIN US Services, LLC."
    - "Sales Invoice"
  any_of:
    - "Invoice Detail"
    - "Invoice No.:"

metadata:
  invoice_number:
    patterns:
      - 'Invoice No\.:\s*([A-Z0-9]+)'
    required: true
  date:
    patterns:
      - 'Invoice Date:\s*(\d{4}-\d{2}-\d{2})'
  supplier_name:
    value: "SHEIN"
  country_code:
    value: "US"
  total:
    patterns:
      - 'Grand Total:\s*([\d,]+\.?\d*)'
    type: currency

items:
  strategy: line
  line:
    pattern: '^(.+?)\s+(\d+)\s+([\d,]+\.?\d*)\s*$'
    field_map:
      description: 1
      quantity: 2
      unit_price: 3
    types:
      quantity: integer
      unit_price: currency
    skip_patterns:
      - '^Item\(s\) Subtotal:'
      - '^Shipping/Handling:'
      - '^Sales Tax:'
      - '^Grand Total:'
      - '^Sold By:'
      - '^Address:'
      - '^\d{4,}'
      - '^Bill To:'
      - '^Order Information'

sections:
  items_start:
    - "Invoice Detail"
  items_end:
    - "Item(s) Subtotal"

validation:
  min_items: 1
  max_item_price: 10000