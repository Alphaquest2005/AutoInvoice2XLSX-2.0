# Auto-generated format spec for shein
# Generated by LLM on 2026-02-13T04:13:51.043499

name: shein_us
version: "1.0"
description: "SHEIN US Sales Invoice format featuring Invoice No., Invoice Date, Sold By, and itemized lists embedded within Invoice Detail sections."

detect:
  all_of:
    - "SHEIN US Services, LLC."
  any_of:
    - "Sales Invoice"
    - "Invoice Detail"

metadata:
  invoice_number:
    patterns:
      - 'Invoice No\.:\s*(INVUS[\d\s]+)'
    required: true
  date:
    patterns:
      - 'Invoice Date:\s*(\d{4}-\d{2}-\d{2})'
    required: true
  supplier_name:
    value: "SHEIN"
  country_code:
    value: "US"
  total:
    patterns:
      - 'Grand Total:\s*([\d,]+\.\d{2})'
    type: currency

items:
  strategy: line
  line:
    pattern: '^(.+?)(?:\s+(\d+)\s+)?([\d,]+\.\d{2})\s*$'
    field_map:
      description: 1
      quantity: 2
      unit_price: 3
    types:
      quantity: integer
      unit_price: currency
    skip_patterns:
      - '^Item\(s\) Subtotal'
      - '^Shipping/Handling'
      - '^Sales Tax'
      - '^Grand Total'
      - '^Order Number'
      - '^Order Date'
      - '^Sold By'
      - '^Address'
      - '^Invoice No'
      - '^Bill To'
      - '^Delivery Address'
      - '^\d{4}-\d{2}-\d{2}'
      - '^\s*$'

sections:
  items_start:
    - "Invoice Detail"
  items_end:
    - "Item(s) Subtotal"

validation:
  min_items: 1
  max_item_price: 10000