# Auto-generated format spec for shein
# Generated by LLM on 2026-02-13T04:19:03.354673

name: shein_us
version: "1.0"
description: "Invoice format for SHEIN US Services, LLC."

detect:
  all_of:
    - "SHEIN US Services, LLC."
    - "777 S Alameda St"
    - "Bill To:"
  any_of:
    - "Sales Invoice"
    - "Invoice Detail"

metadata:
  invoice_number:
    patterns:
      - 'Invoice No\.:\s*INVUS\d+\s+(\d+)'
    required: true
  date:
    patterns:
      - 'Invoice Date:\s*\n\s*\d{1,3}\s+\d{4}-(\d{2}-\d{2})'
      - '(\d{4}-\d{2}-\d{2})'
    required: true
  supplier_name:
    value: "SHEIN US Services, LLC."
  country_code:
    value: "US"
  total:
    patterns:
      - 'Grand Total:\s*[\$\s]*([\d,]+\.\d{2})'
    type: currency

items:
  strategy: line
  line:
    pattern: '^(.{10,}?)\s+(\d+)\s+([\d,]+\.\d{2})$'
    field_map:
      description: 1
      quantity: 2
      unit_price: 3
    types:
      quantity: integer
      unit_price: currency
    skip_patterns:
      - '^Invoice Detail'
      - '^Item\(s\) Subtotal'
      - '^Shipping/Handling'
      - '^Sales Tax'
      - '^Grand Total'
      - '^Order Number'
      - '^Order Date'
      - '^Sold By'
      - '^Address'
      - '^Bill To'
      - '^Billing Address'
      - '^Delivery Address'

sections:
  items_start:
    - "Invoice Detail"
  items_end:
    - "Item(s) Subtotal"

validation:
  min_items: 1
  max_item_price: 10000