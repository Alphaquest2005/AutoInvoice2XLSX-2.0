# Auto-generated format spec for shein
# Generated by LLM on 2026-02-13T04:51:05.257390

name: shein_us
version: "1.0"
description: "SHEIN US Invoice format with specific layout for multi-line item descriptions and Item(s) Subtotal summary."

detect:
  all_of:
    - "SHEIN US Services"
    - "Invoice Detail"
  any_of:
    - "Item(s) Subtotal:"

metadata:
  invoice_number:
    patterns:
      - 'Invoice No\.:\s*([A-Z0-9]+)'
    required: true
  date:
    patterns:
      - 'Invoice Date:\s*(\d{4}-\d{2}-\d{2})'
  supplier_name:
    value: "SHEIN US Services"
  country_code:
    value: "US"
  total:
    patterns:
      - 'Grand Total:[\s\S]*?([\d,]+\.\d{2})'
    type: currency

items:
  strategy: line
  line:
    pattern: '^(10|20|40|50)\/(20|50)PCS\s+(.+?)(?:,\s+(.+?))?,?(\s\d+)?(?:\s\d+)?$'
    field_map:
      quantity: 1
      description: 3
      unit_price: 5
    types:
      quantity: integer
      unit_price: currency
    skip_patterns:
      - '^Order Information'
      - '^Seller Information'
      - '^Sales Invoice'
      - '^Item\(s\) Subtotal'
      - '^Shipping/Handling'
      - '^Sales Tax'
      - '^Grand Total'

sections:
  items_start:
    - "Invoice Detail"
  items_end:
    - "Item(s) Subtotal"

validation:
  min_items: 1