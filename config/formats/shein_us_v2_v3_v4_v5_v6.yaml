# Auto-generated format spec for shein
# Generated by LLM on 2026-02-13T05:02:29.630961

name: shein_us
version: "1.0"
description: "SHEIN US Services, LLC. Invoices featuring items, shipping, and tax breakdowns."

detect:
  all_of:
    - "SHEIN US Services"
  any_of:
    - "Sales Invoice"
    - "Invoice Detail"

metadata:
  invoice_number:
    patterns:
      - 'Invoice No\.:\s*(INV[A-Z0-9]+)'
    required: true
  date:
    patterns:
      - 'Invoice Date:\s*(\d{4}-\d{2}-\d{2})'
  supplier_name:
    value: "SHEIN"
  country_code:
    value: "US"
  total:
    patterns:
      - 'Grand Total:\s*([\d,]+\.?\d*)'
    type: currency

items:
  strategy: line
  line:
    pattern: '^(?:(SHEIN|Fashionable|Women''s|7-Pack|Sports).+?)(?:(\d+)\s+)?([\d,]+\.?\d*)$'
    field_map:
      description: 1
      quantity: 2
      unit_price: 3
    types:
      quantity: integer
      unit_price: currency
    skip_patterns:
      - '^Item\(s\) Subtotal'
      - '^Shipping/Handling'
      - '^Sales Tax'
      - '^Grand Total'
      - '^Order Number'
      - '^Order Date'
      - '^Invoice No'
      - '^Invoice Date'
      - '^Bill To'
      - '^Delivery Address'
      - '^Sold By'
      - '^Address'
      - '^https?://'
      - '\.jpg|\.png'
      - '^SHEIN US Services'
      - '^777 S Alameda'
      - '^IMG-'

sections:
  items_start:
    - "Invoice Detail"
  items_end:
    - "Grand Total"

validation:
  min_items: 1