# Auto-generated format spec for shein
# Generated by LLM on 2026-02-13T08:54:45.230748

name: shein_us
version: "1.0"
description: "Invoice format for SHEIN US Services, LLC"

detect:
  all_of:
    - "SHEIN US Services, LLC"
    - "Bill To:"
    - "Invoice Detail"

metadata:
  invoice_number:
    patterns:
      - 'Invoice No\.:\\s*(INV\\d+\\s+\\d+)'
    required: true
  date:
    patterns:
      - 'Invoice Date:\\s*(\\d{4}-\\d{2}-\\d{2})'
  supplier_name:
    value: "SHEIN US Services, LLC"
  country_code:
    value: "US"
  total:
    patterns:
      - 'Grand Total:\\s*([\\d,]+\\.\\d{2})'
    type: currency

items:
  strategy: line
  line:
    pattern: '^(.+?)\\s+(\\d+)\\s+([\\d,]+\\.\\d{2})\\s*$'
    field_map:
      description: 1
      quantity: 2
      unit_price: 3
    types:
      quantity: integer
      unit_price: currency
    skip_patterns:
      - '^Item\\(s\\) Subtotal:'
      - '^Shipping/Handling:'
      - '^Sales Tax:'
      - '^Grand Total:'
      - '^Order Number:'
      - '^Order Date:'
      - '^Sold By:'
      - '^Address:'
      - '^\\s*$'
    generated_fields:
      sku: "SHEIN-{invoice_number}-{index}"

sections:
  items_start:
    - "Invoice Detail"
  items_end:
    - "Item(s) Subtotal"

validation:
  min_items: 1