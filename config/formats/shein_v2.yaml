# Auto-generated format spec for shein
# Generated by LLM on 2026-02-13T04:46:39.304333

name: shein
version: "1.0"
description: "Invoice format for SHEIN US Services, LLC featuring order info, seller details, and tabular invoice details."

detect:
  all_of:
    - "SHEIN US Services, LLC."
  any_of:
    - "Invoice Detail"
    - "Grand Total:"
    - "Invoice No.:"

metadata:
  invoice_number:
    patterns:
      - 'Invoice No\.:\s*([A-Z0-9]+)'
    required: true
  date:
    patterns:
      - 'Invoice Date:\s*(\d{4}-\d{2}-\d{2})'
  supplier_name:
    value: "SHEIN US Services, LLC."
  country_code:
    value: "US"
  total:
    patterns:
      - 'Grand Total:\s*([\d,]+\.\d{2})'
    type: currency

items:
  strategy: line
  line:
    pattern: '^(.*?)\s+(\d+)\s+([\d,]+\.\d{2})\s*$'
    field_map:
      description: 1
      quantity: 2
      unit_price: 3
    types:
      quantity: integer
      unit_price: currency
    skip_patterns:
      - '^Description'
      - '^Item\(s\) Subtotal:'
      - '^Shipping/Handling:'
      - '^Sales Tax:'
      - '^Grand Total:'
      - '^Order Information'
      - '^Seller Information'

sections:
  items_start:
    - "Invoice Detail"
  items_end:
    - "Item(s) Subtotal"

validation:
  min_items: 1
  max_item_price: 10000