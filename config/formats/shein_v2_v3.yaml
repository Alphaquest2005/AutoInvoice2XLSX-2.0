# Auto-generated format spec for shein
# Generated by LLM on 2026-02-13T04:55:58.229351

name: shein
version: "1.0"
description: "SHEIN Invoice format featuring multi-column item lists, INVUS invoice numbers, and specific footer calculations."

detect:
  all_of:
    - "SHEIN"
    - "Sales Invoice"
  any_of:
    - "SHEIN US Services"
    - "Invoice Detail"
    - "Shipping/Hand"

metadata:
  invoice_number:
    patterns:
      - 'Invoice No\.?:\s*([A-Z0-9\s]+)'
      - 'INVUS\s*([0-9\s]+)'
    required: true
  date:
    patterns:
      - 'Invoice Date:\s*([0-9]{4}-[0-9]{2}-[0-9]{2})'
      - 'invoice Date:\s*([0-9]{4}-[0-9]{2}-[0-9]{2})'
  supplier_name:
    value: "SHEIN"
  country_code:
    value: "US"
  total:
    patterns:
      - 'Grand Total:\s*([\d,]+\.?\d*)'
    type: currency

items:
  strategy: line
  line:
    pattern: '(?i)^(?P<description>.+?)\s+(?P<quantity>\d+)\s+(?P<unit_price>[\d,]+\.?\d*)$'
    field_map:
      description: description
      quantity: quantity
      unit_price: unit_price
    types:
      quantity: integer
      unit_price: currency
    skip_patterns:
      - '^Item\(s\) Subtotal'
      - '^Shipping'
      - '^Sales Tax'
      - '^Grand Total'
      - '^Description\s+(Quantity|Amount)'
      - '^Amount'
      - '^\s*$'
      - '^SHEIN'
    generated_fields:
      sku: "SHEIN-{index}"

sections:
  items_start:
    - "Invoice Detail"
  items_end:
    - "Item(s) Subtotal"

validation:
  min_items: 1
  max_item_price: 10000