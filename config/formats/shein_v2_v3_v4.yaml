# Auto-generated format spec for shein
# Generated by LLM on 2026-02-13T08:53:35.069185

name: shein
version: "1.0"
description: "Invoice format for SHEIN orders, featuring multi-line item descriptions and 'Invoice Detail' section."

detect:
  all_of:
    - "Invoice Detail"
    - "Amount(USD)"
    - "Order Number:"
  any_of:
    - "SHEIN DISTRIBUTION CORPORATION"
    - "Order Information"

metadata:
  invoice_number:
    patterns:
      - 'Order Number:\s*([A-Z0-9]+)'
    required: true
  date:
    patterns:
      - '(\d{4}-\d{2}-\d{2})'
  supplier_name:
    value: "SHEIN"
  country_code:
    value: "US"
  total:
    patterns:
      - 'Grand Total:\s*([\d,]+?\.\d{2})'
    type: currency

items:
  strategy: line
  line:
    pattern: '^([A-Za-z0-9].*?)\s+(\d+)\s+([\d,]+?\.\d{2})\s*$'
    field_map:
      description: 1
      quantity: 2
      unit_price: 3
    types:
      quantity: integer
      unit_price: currency
    skip_patterns:
      - '^Item\(s\) Subtotal:'
      - '^Shipping/Handling:'
      - '^Sales Tax:'
      - '^Grand Total:'
      - '^Description\s+(Quantity|Amount)'
      - '^Invoice Detail'
      - '^Sold By:'
      - '^Order Number:'
    generated_fields:
      sku: "SHEIN-{index}"

sections:
  items_start:
    - "Invoice Detail"
  items_end:
    - "Grand Total"

validation:
  min_items: 1