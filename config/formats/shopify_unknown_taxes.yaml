# Auto-generated format spec for unknown_taxes
# Generated by LLM on 2026-02-12T23:08:50.980909

name: shopify_unknown_taxes
version: "1.0"
description: "Shopify invoice with specific layout including footer links and item groupings"

detect:
  all_of:
    - "Payment method: shopify_payments"
    - "Order #"
  any_of:
    - "SHOP FASTER WITH THE APP"
    - "QUICK LINKS"

metadata:
  invoice_number:
    patterns:
      - 'Order #(\d+)'
    required: true
  date:
    patterns:
      - 'Date Placed:\s*([A-Za-z]{3}\s\d{1,2}/\d{4})'
  supplier_name:
    value: "unknown_taxes"
  country_code:
    value: "US"
  total:
    patterns:
      - 'Total:\s*([$]\d+\.\d{2})'
    type: currency

items:
  strategy: line
  line:
    pattern: '^(?P<description>.+?)\s+-\s+(?P<variant>[^\$]+)\s+(?P<unit_price>[$]\d+\.\d{2})(?:\s+Size:\s*.*?)?(?:\s+(?:Oty|Qty):\s*(?P<quantity>\d+))?'
    field_map:
      description: 1
      unit_price: 3
      quantity: 4
    types:
      quantity: integer
      unit_price: currency
    skip_patterns:
      - '^(Size:|Oty:|Qty:|Payment method:|Shipping|Delivery Address|Billing Address|Total)'
      - '\| want to edit'
      - 'Going Viral'
      - 'Ready To Slay'
      - 'Lorena Slinky'
      - 'Worth It Ribbed'
      - 'Always Adored'
      - 'Out Of Your League'
      - 'Feeling Brand New'
      - '2 Pack Panties'
      - 'Keep On Slashing'
      - 'Nova Season'
      - '\$[\d,]+\.\d{2}'
      - '^\+\s*[$]'

sections:
  items_start:
    - "AllOrders"
  items_end:
    - "Payment method"

validation:
  min_items: 1