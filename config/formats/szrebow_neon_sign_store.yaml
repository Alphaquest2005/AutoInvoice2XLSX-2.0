# Auto-generated format spec for unknown_invoice
# Generated by LLM on 2026-02-13T04:51:15.968817

name: SZREBOW_Neon_Sign_Store
version: "1.0"
description: "Invoice format for SZREBOW Neon Sign Store via Alibaba.com"

detect:
  all_of:
    - "SZREBOW Neon Sign Store"
  any_of:
    - "Alibaba.com Singapore E-Commerce"
    - "Marketplace Facilitator"

metadata:
  invoice_number:
    patterns:
      - 'Invoice No\.\s*[\n\s]+([A-Z0-9]+)'
      - '(B[A-Z0-9]{15,})'
    required: false
  date:
    patterns:
      - '([A-Z][a-z]{2}\s[A-Z][a-z]{2}\s\d{1,2}\s\d{4})'
  supplier_name:
    value: "SZREBOW Neon Sign Store"
  country_code:
    value: "SG"
  total:
    patterns:
      - 'Total\s+\w+\s+of\s+Sales\s+Tax[^)]*\)\s*([\d,]+\.?\d*)'
      - '(\d[\d,.]+)\)\s*$'
    type: currency

items:
  strategy: line
  line:
    # Price line format: UNIT_PRICE) TAX_RATE % TAX_AMOUNT TOTAL)
    # OCR artifacts: closing parens ")" appear after prices
    # e.g. "190.00) 7.000 % 0 203.30)"
    pattern: '(\d[\d,.]*)\)\s+[\d.]+\s*%\s+\d+\s+(\d[\d,.]*)\)'
    field_map:
      unit_price: 1
      total_cost: 2
    types:
      unit_price: currency
      total_cost: currency
    skip_patterns:
      - '^Sub total'
      - '^Total'
      - 'Invoice No\.'
      - '^\s*$'
    generated_fields:
      quantity: 1
      sku: "SZREBOW-{index}"

sections:
  items_start:
    - "Transaction"
    - "Delivery Address"
  items_end:
    - "required by"
    - "tax authorities"

validation:
  min_items: 1
  max_item_price: 5000