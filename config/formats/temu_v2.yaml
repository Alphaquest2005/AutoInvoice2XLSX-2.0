# Auto-generated format spec for temu
# Generated by LLM on 2026-02-13T04:36:49.177797

name: temu
version: "1.0"
description: "Temu invoice format with order summary and item details"

detect:
  all_of:
    - "Order ID:"
    - "Order time:"
    - "Temu"
  any_of:
    - "Item details"
    - "Order Summary"

metadata:
  invoice_number:
    patterns:
      - 'Order ID:\s*([A-Z0-9-]+)'
    required: true
  date:
    patterns:
      - 'Order time:\s*([A-Za-z]+\s+\d{1,2},\s+\d{4})'
  supplier_name:
    value: "Temu"
  country_code:
    value: "US"
  total:
    patterns:
      - 'Order total:\s*[$]?(\d+\.\d{2})'
    type: currency

items:
  strategy: line
  line:
    pattern: '(.+?)\s+\$(\d+\.\d{2})$'
    field_map:
      description: 1
      unit_price: 2
    types:
      unit_price: currency
    skip_patterns:
      - '^Order Summary'
      - '^Order ID:'
      - '^Order time:'
      - '^item\(s\) total'
      - '^ttem\(s\) discount'
      - '^Subtotal:'
      - '^Shipping:'
      - '^Sales tax:'
      - '^Credit:'
      - '^Ordor total:'
      - '^Shipping address'
      - '^Item details'
      - '^\(\d+\)$'
      - '^\(C\)'
      - '^By\s'
      - '^Free returns'
      - '^\d+/\d+$'
      - '^Sign Out'
      - 'Conditions of Use'
      - 'Privacy Notice'
      - '©'

sections:
  items_start:
    - "Item details"
  items_end:
    - "Free returns"

validation:
  min_items: 1
  max_item_price: 10000