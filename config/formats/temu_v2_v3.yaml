# Auto-generated format spec for temu
# Generated by LLM on 2026-02-13T08:52:58.199112

name: temu
version: "1.0"
description: "Temu Order Invoice/Receipt Format"

detect:
  all_of:
    - "Order Summary"
    - "Order ID:"
    - "Temu does not share your Information"
  any_of:
    - "Item detalls"
    - "Your payment Information ts safe with us"

metadata:
  invoice_number:
    patterns:
      - 'Order ID:\s*(PO-\d{3}-\d{17})'
    required: true
  date:
    patterns:
      - 'Order time:\s*([A-Za-z]{3}\s+\d{2},\d{4})'
      - 'Paid on\s+([A-Za-z]{3}\s+\d{2},\s+\d{4})'
  supplier_name:
    value: "Temu"
  country_code:
    value: "US"
  total:
    patterns:
      - 'Order total:\s*\$?([\d,]+\.\d{02})'
    type: currency
  shipping:
    patterns:
      - 'Shipping:\s*\$?([\d,]+\.\d{02})'
    type: currency
  tax:
    patterns:
      - 'Salos tax:\s*\$?([\d,]+\.\d{02})'
    type: currency

items:
  strategy: line
  line:
    pattern: '^(.+?)\s+\$?([\d,]+\.\d{02})$'
    field_map:
      description: 1
      unit_price: 2
    types:
      unit_price: currency
    skip_patterns:
      - '^Order total'
      - '^Shipping'
      - '^Salos tax'
      - '^Subtotal'
      - '^item\(s\) total'
      - '^\s*$'
      - '^By '
      - '^Free returns'
      - '^\d{1,}/\d{1,}$'
      - '^\('
    generated_fields:
      sku: "TEMU-{index}"

sections:
  items_start:
    - "Item detalls"
  items_end:
    - "Free returns within"

validation:
  min_items: 1