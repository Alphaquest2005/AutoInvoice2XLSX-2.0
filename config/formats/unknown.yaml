# Auto-generated format spec for unknown_
# Generated on 2026-04-04T00:34:29.929610
# Will be promoted to formats/ on successful processing

name: unknown_
version: "1.0"
description: "Invoice format for unknown_ with itemized product lines and prices"

detect:
  all_of:
    - "By"
  any_of:
    - "COJAY"
    - "Case Honour"

metadata:
  invoice_number:
    patterns:
      - 'Invoice[:\s]*([A-Z0-9-]+)'
    required: false
  date:
    patterns:
      - '\d{1,2}[/-]\d{1,2}[/-]\d{2,4}'
      - '(?:Jan|Feb|Mar|Apr|May|Jun|Jul|Aug|Sep|Oct|Nov|Dec)[a-z]*\s+\d{1,2},\s+\d{4}'
  supplier_name:
    value: "unknown_"
  country_code:
    value: "US"
  total:
    patterns:
      - 'Total[:\s]*[$]?\s*([\d,]+\.\d{2})'
    type: currency

items:
  strategy: line
  line:
    pattern: '^(.+?)\s+\$([\d,]+\.\d{2})\s*$'
    field_map:
      description: 1
      unit_price: 2
    types:
      unit_price: currency
    skip_patterns:
      - '^By\s+'
      - '^For\s+'
      - '^Total'
      - '^Subtotal'
      - '^Tax'
      - '^Shipping'
      - '^\s*\d+\s+x\d+\s*$'

sections:
  items_start:
    - "Luxurious"
    - "Faux"
    - "Leather"
  items_end:
    - "Total"

validation:
  min_items: 1