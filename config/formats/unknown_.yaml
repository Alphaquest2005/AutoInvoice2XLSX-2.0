# Auto-generated format spec for unknown_
# Generated by LLM on 2026-02-13T05:13:04.722272

name: unknown_
version: "1.0"
description: "Invoice format for unknown_ (Raw/Scrambled Text)"

detect:
  all_of:
    - "Nugqus"
    - "\\. J Sy"
  any_of:
    - "\\."

metadata:
  invoice_number:
    patterns:
      - '(?i)invoice[:#\\s]+([a-z0-9-]+)'
      - '(?i)number[:#\\s]+([a-z0-9-]+)'
  date:
    patterns:
      - '(?i)date[:\\s]+(\\d{1,2}[/-]\\d{1,2}[/-]\\d{2,4})'
      - '(\\d{4}-\\d{2}-\\d{2})'
  total:
    patterns:
      - '(?i)total[:\\s]+([\\d,]+\\.\\d{2})'
    type: currency

items:
  strategy: line
  line:
    pattern: '^([\\w\\s]+).*?([\\d,]+\\.\\d{2})$'
    field_map:
      description: 1
      unit_price: 2
    types:
      unit_price: currency
    skip_patterns:
      - '^Subtotal'
      - '^Tax'
      - '^Total'
      - '^Shipping'
      - '^Balance'
      - '^\\s*$'

sections:
  items_start:
    - "Description"
    - "Item"
  items_end:
    - "Total"
    - "Amount Due"

validation:
  min_items: 0