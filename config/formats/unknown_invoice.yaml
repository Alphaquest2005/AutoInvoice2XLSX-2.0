# Auto-generated format spec for unknown_invoice
# Generated on 2026-04-06T11:48:06.744379
# Will be promoted to formats/ on successful processing

name: unknown_invoice
version: "1.0"
description: "Invoice format with multi-page headers, 'Line Item Ordered Shipped Price Extension' columns, and sub-items (e.g., 2.1, 2.2)."

detect:
  all_of:
    - "Line Item Ordered Shipped Price Extension"
  any_of:
    - "Salesperson"
    - "Ship Via"

metadata:
  invoice_number:
    patterns:
      - 'Invoice\s+(\d+)'
    required: true
  date:
    patterns:
      - 'Date\s+(\d{2}/\d{2}/\d{4})'
  supplier_name:
    value: "Unknown Supplier"
  country_code:
    value: "US"
  total:
    patterns:
      - 'Total\s+([\d,]+\.\d{2})'
    type: currency

items:
  strategy: line
  line:
    pattern: '^\s*(\d+(?:\.\d+)?)\s+\S+\s+(\d+)Unit\s+(\d+)\s+([\d.]+)Unit\s+([\d.]+)'
    field_map:
      sku: 1
      quantity: 3
      unit_price: 4
      total_price: 5
    types:
      quantity: integer
      unit_price: currency
      total_price: currency
    skip_patterns:
      - '^Line Item'
      - '^Page \d+ of \d+'
      - '^Invoice\s+\d+'
      - '^Order\s+\d+'
      - '^Salesperson'
      - '^Sold To:'
      - '^Ship To:'
      - '^Ship Via'
      - '^UPC:'
      - '^-'
      - '^DISCONTINUED'