# Auto-generated format spec for unknown_order
# Generated by LLM on 2026-02-13T04:09:30.743875

name: unknown_order
version: "1.0"
description: "Invoice format for unknown_order marketplace orders"

detect:
  all_of:
    - "Items bought from"
    - "Order number:"
    - "Order total"

metadata:
  invoice_number:
    patterns:
      - 'Order number:\s*([\d\-]+)'
    required: true
  date:
    patterns:
      - 'Placed on\s+([A-Za-z]{3}\s+\d{1,2},\s+\d{4})'
  supplier_name:
    patterns:
      - 'Items bought from\s+(.+)'
  total:
    patterns:
      - 'Order total\s+([\d,]+\.\d{2})'
    type: currency

items:
  strategy: line
  line:
    pattern: '^(\d+)\s+(.+?)\s+([A-Za-z].+?)\s+\$\d+\.\d{2}$'
    field_map:
      quantity: 1
      description: 2
      sku: 3
    types:
      quantity: integer
    skip_patterns:
      - '^Items bought from'
      - '^Quantity ftem name'
      - '^\s*$'

sections:
  items_start:
    - "Items bought from"
  items_end:
    - "Order total"

validation:
  min_items: 1