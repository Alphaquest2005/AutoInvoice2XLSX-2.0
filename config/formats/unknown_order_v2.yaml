# Auto-generated format spec for unknown_order
# Generated by LLM on 2026-02-13T04:09:50.422093

name: unknown_order
version: "1.0"
description: "Invoice format for unknown_order with Order number and specific item layout"

detect:
  all_of:
    - "Order number:"
    - "Items bought from"
    - "Order total"

metadata:
  invoice_number:
    patterns:
      - 'Order number:\s*([0-9-]+)'
    required: true
  date:
    patterns:
      - 'Placed on\s+([A-Za-z]{3}\s+\d{1,2},\s+\d{4})'
  supplier_name:
    patterns:
      - 'Items bought from\s+([A-Za-z0-9-]+)'
  country_code:
    value: "US"
  total:
    patterns:
      - 'Order total\s+\$?([\d,]+\.\d{2})'
    type: currency

items:
  strategy: line
  line:
    pattern: '^(\d+)\s+(.+?)\s+([A-Za-z].+?)\s+\$([\d,]+\.\d{2})\s*$'
    field_map:
      quantity: 1
      description: 2
      sku: 3
      unit_price: 4
    types:
      quantity: integer
      unit_price: currency
    skip_patterns:
      - '^Order information'
      - '^Shipping address'
      - '^Order total'
      - '^Quantity item name'
      - '^Items bought'
      - '^$'
      - '^\s*$'

sections:
  items_start:
    - "Quantity item name"
  items_end:
    - "Order total"

validation:
  min_items: 1
  max_item_price: 10000