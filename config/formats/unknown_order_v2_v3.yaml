# Auto-generated format spec for unknown_order
# Generated by LLM on 2026-02-13T04:32:40.856532

name: unknown_order
version: "1.0"
description: "Unknown order invoice format with item table and metadata"

detect:
  any_of:
    - "Order information"
    - "Shipping address"
    - "Items bought from"

metadata:
  invoice_number:
    patterns:
      - 'Order number:\s*([\w-]+)'
    required: true
  date:
    patterns:
      - 'Placed on\s+(\w+\s+\d{1,2},\s+\d{4})'
  supplier_name:
    patterns:
      - 'Items bought from\s+(.+)'
  total:
    patterns:
      - 'Order total\s+[\$]([\d,]+\.\d{2})'
    type: currency

items:
  strategy: line
  line:
    pattern: '^(\d+)\s+(.+?)\s+(?:\([\d]+\)\s+)?(.+?)\s+[\$]([\d,]+\.\d{2})$'
    field_map:
      quantity: 1
      sku: 2
      description: 3
      unit_price: 4
    types:
      quantity: integer
      unit_price: currency
    skip_patterns:
      - '^Quantity\s+f?tem name'
      - '^Items bought from'
      - '^Order information'
      - '^Shipping address'
      - '^Order total'
      - '^\s*$'

sections:
  items_start:
    - "Quantity ftem name"
  items_end:
    - "Order total"

validation:
  min_items: 1