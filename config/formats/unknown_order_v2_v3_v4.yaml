# Auto-generated format spec for unknown_order
# Generated by LLM on 2026-02-13T04:32:51.788015

name: unknown_order
version: "1.0"
description: "Standard order confirmation format from an unknown supplier (koduste), featuring buyer/seller info, shipping address, and a distinct item section with quantity, name, and order totals."

detect:
  all_of:
    - "Order information"
    - "Items bought from"
    - "Order number:"
  any_of:
    - "Order total"

metadata:
  invoice_number:
    patterns:
      - 'Order number:\s*([\d-]+)'
    required: true
  date:
    patterns:
      - 'Placed on\s+(\w+\s+\d+,\s+\d{4})'
      - 'Paid on\s+(\w+\s+\d+,\s+\d{4})'
  supplier_name:
    patterns:
      - 'Seller\s+(\w+)'
  total:
    patterns:
      - 'Order total\s+[\$]?([\d,]+\.\d{2})'
    type: currency

items:
  strategy: line
  line:
    pattern: '^(\d+)\s+(.+?)(?:\s+\(([\d]+)\))?$'
    field_map:
      quantity: 1
      description: 2
      sku: 3
    types:
      quantity: integer
    skip_patterns:
      - '^Items bought from'
      - '^Order number'
      - '^Order total'
      - '^\s*\d+\s+items\s+'
      - '^Shipping\s+'
      - '^Tax'
      - '^Item\s+'
      - '^Shipping service'
      - '^\w+\s+Standard'

sections:
  items_start:
    - "Quantity Item name"
  items_end:
    - "Order total"

validation:
  min_items: 1