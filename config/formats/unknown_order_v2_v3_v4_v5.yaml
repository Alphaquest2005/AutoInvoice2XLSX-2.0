# Auto-generated format spec for unknown_order
# Generated by LLM on 2026-02-13T04:33:00.318059

name: unknown_order
version: "1.0"
description: "Order format for unknown_order supplier"

detect:
  all_of:
    - "Order information"
    - "Shipping address"
    - "Order total"
  any_of:
    - "Items bought from"

metadata:
  invoice_number:
    patterns:
      - 'Order number:\s*([\d-]+)'
    required: true
  date:
    patterns:
      - 'Placed on\s+([A-Za-z]{3}\s+\d{1,2},\s+\d{4})'
  supplier_name:
    patterns:
      - 'Items bought from\s+([^\n]+)'
  country_code:
    value: "US"
  total:
    patterns:
      - 'Order total\s+\$?([\d,]+\.?\d*)'
    type: currency

items:
  strategy: line
  line:
    pattern: '^(\d+)\s+(.+?)\s+(.+?)\s+\$?([\d,]+\.?\d*)$'
    field_map:
      quantity: 1
      description: 2
      shipping_service: 3
      unit_price: 4
    types:
      quantity: integer
      unit_price: currency
    skip_patterns:
      - '^Order number'
      - '^Quantity'
      - '^Items bought'
    generated_fields:
      sku: "ITEM-{index}"

sections:
  items_start:
    - "Quantity item name"
  items_end:
    - "Order total"

validation:
  min_items: 1