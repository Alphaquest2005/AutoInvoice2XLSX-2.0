# Auto-generated format spec for unknown_solid
# Generated on 2026-03-17T09:37:10.278136
# Will be promoted to formats/ on successful processing

name: unknown_solid
version: "1.0"
description: "Invoice format for unknown_solid with items split across lines (description, attributes, supplier, price, quantity)."

detect:
  all_of:
    - "By"
  any_of:
    - "Pairs"
    - "Label size"

metadata:
  invoice_number:
    patterns:
      - 'N/A'
    required: false
  date:
    patterns:
      - 'N/A'
  supplier_name:
    value: "unknown_solid"
  country_code:
    value: "US"
  total:
    patterns:
      - '\$[\d,]+\.\d{2}'
    type: currency

items:
  strategy: line
  line:
    pattern: '^(?P<description>.+?)\s+\$(?P<unit_price>[\d,]+\.\d{2})$'
    field_map:
      description: 1
      unit_price: 2
    types:
      unit_price: currency
    skip_patterns:
      - '^By '
      - '^\d+g \/ \d+$'
      - '^\w+ \+ \w+$'
      - '^Color '
      - '^\w+\/\w+'
      - '^\w+ \- \w+$'
      - '^\w+ \/ Label size:'
      - '^\( \d+$'
      - '^Pairs$'
      - '^\d+$'
    generated_fields:
      quantity: 1
      sku: "ITEM-{index}"

sections:
  items_start:
    - "Solid Notch"
  items_end:
    - "Total"

validation:
  min_items: 1
  max_item_price: 100