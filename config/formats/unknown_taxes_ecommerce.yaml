# Auto-generated format spec for unknown_taxes
# Generated by LLM on 2026-02-13T08:49:57.720317

name: unknown_taxes_ecommerce
version: "1.0"
description: "Fashion e-commerce invoice with specific text layout including Payment method, Size/Oty lines, and Quick Links."

detect:
  all_of:
    - "Payment method:"
  any_of:
    - "QUICK LINKS"
    - "Shopify"
    - "Order #"

metadata:
  invoice_number:
    patterns:
      - 'Order\s+#(\d+)'
    required: true
  date:
    patterns:
      - 'Date Placed:\s*([A-Za-z]+\s+\d{1,2}/\d{4})'
  supplier_name:
    value: "unknown_taxes"
  country_code:
    value: "US"
  total:
    patterns:
      - 'Total:\s*\$[\d,]+\.?\d*\s*([\$]?\d{1,3}(?:,\d{3})*\.\d{2})'
    type: currency

items:
  strategy: line
  line:
    pattern: '([A-Z][\w\s]+?)\s+(-?\s?\$[\d,]+\.?\d*)'
    field_map:
      description: 1
      unit_price: 2
    types:
      unit_price: currency
    skip_patterns:
      - '^Size:'
      - '^Oty'
      - '^\d+\s+Pack'
      - '^\$'
      - 'FREE'
      - '\+$'
      - 'Order #'
      - 'Date Placed:'
    generated_fields:
      sku: "ITEM-{index}"

sections:
  items_start:
    - "Order #"
    - "Date Placed:"
  items_end:
    - "Total"

validation:
  min_items: 1