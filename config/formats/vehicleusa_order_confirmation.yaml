# Auto-generated format spec for unknown_order
# Generated by LLM on 2026-02-13T05:11:55.371359

name: vehicleusa_order_confirmation
version: "1.0"
description: "Order confirmation format for eBay/vehicleusa style invoices with specific item table layout"

detect:
  all_of:
    - "Items bought from"
    - "Order number:"
  any_of:
    - "Order information"
    - "Shipping address"

metadata:
  invoice_number:
    patterns:
      - 'Order number:\s*([0-9-]+)'
    required: true
  date:
    patterns:
      - 'Placed on\s+([A-Z][a-z]{2}\s+\d{1,2},\s+\d{4})'
  supplier_name:
    value: "vehicleusa"
  country_code:
    value: "US"
  total:
    patterns:
      - 'Order total\s+\$?([\d,]+\.\d{2})'
    type: currency

items:
  strategy: line
  line:
    pattern: '^(\d{7,20})\s+(\d+)\s+(.*?)\s+(.*?)\s+\$([\d,]+\.\d{2})$'
    field_map:
      sku: 1
      quantity: 2
      description: 3
      shipping_service: 4
      unit_price: 5
    types:
      quantity: integer
      unit_price: currency
    skip_patterns:
      - '^Item'
      - '^Item name'
      - '^Quantity'
      - '^\s*$'
    generated_fields:
      line_id: "{sku}"

sections:
  items_start:
    - "Items bought from"
  items_end:
    - "Order total"

validation:
  min_items: 1
  max_item_price: 10000